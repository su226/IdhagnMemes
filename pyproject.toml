[project]
name = "idhagnmemes"
version = "0.1.0"
description = "从 IdhagnBot 里分离出来的梗图，用于 meme-generator 加载。 "
readme = "README.md"
authors = [
    { name = "su226", email = "thesu226@outlook.com" }
]
requires-python = ">=3.9"
dependencies = [
    "meme-generator>=0.1.14,<0.2.0",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12"]
build-backend = "uv_build"
